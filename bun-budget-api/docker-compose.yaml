services:
  server:
    build:
      context: ../
      dockerfile: ./bun-budget-api/Dockerfile.dev
    volumes:
      - ./src:/usr/src/app/src
    ports:
      - 8080:8080
    environment:
      - PORT=8080
    restart: unless-stopped
